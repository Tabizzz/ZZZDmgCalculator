{
	"framework": null,
	"buildCommand": "dotnet publish ZZZDmgCalculator --configuration Release --output .build/publish --self-contained",
	"outputDirectory": ".build/publish/wwwroot",
	"installCommand": "dotnet workload install wasm-tools",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/"
		}
	],
	"trailingSlash": false
}